# Local / personal - never published
PROGRESS.md
ui-backup/
.claude/
analyze.js
region.js

# Build output (release zips are uploaded to GitHub Releases, not committed)
dist/
*.zip

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Raw screen recordings (only the converted docs/*.gif is published)
*.mp4
*.mov

# Private handoff notes and local dev tools
HANDOFF.md
dev-tools/

# Desktop tracker app: dependencies and build output
desktop-tracker/node_modules/
desktop-tracker/release/
